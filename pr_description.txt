🎯 **What:** Extracted the batch processing loop body inside `generateEmbeddingsInBatches` into a separate `processBatch` private method, and flattened the embedding dimension check with a `guard` statement using nil-coalescing.
💡 **Why:** Reduces the deep nesting in `EmbeddingService.swift` and makes the dimension checks easier to read.
✅ **Verification:** Visually verified the changes with `cat`. Bypassed tests locally since the Linux environment lacked `xcodebuild` and `swift`, but the changes are syntactically and logically sound.
✨ **Result:** Improved readability and maintainability without altering the processing behavior or exception throwing semantics.
